<div class="container devise-session">
  <div class="devise-session-header text-center mb-4 mt-4">
    <%= render 'shared/logo' %>
    <h2>Log in to Wave</h2>
  </div>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
        required: false,
        autofocus: true,
        input_html: { autocomplete: "email" }
      %>
      <%= f.input :password,
        required: false,
        input_html: { autocomplete: "current-password" }
      %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions text-center mb-3">
      <%= f.button :submit, "Log in", class: 'btn-signup-invert' %>
    </div>
  <% end %>

  <div class="devise-session-links text-center">
    <%= render "devise/shared/links" %>
  </div>
</div>
